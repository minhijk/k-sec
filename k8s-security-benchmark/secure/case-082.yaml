apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: secure-32-secret-reader-limited
  namespace: production
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["app-specific-secret"]
  verbs: ["get"]